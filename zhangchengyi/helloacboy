#include<iostream>
#include<stack>
#include<queue>
#include<string>
using namespace std;
int main()
{
	char s[10];
	int t,n,i,m;
	cin>>t;
	while(t--)
	{
		cin>>n>>s;
		if(s[2]=='F')
		{
			queue<int> q;
			for(i=0;i<n;i++)
			{
				cin>>s;
				if(s[0]=='I')
				{
					cin>>m;
					q.push(m);
				}
				else
				{
					if(q.empty())
					cout<<"None";
					else
					{
					cout<<q.front()<<endl;
					q.pop();
				    }
				}
			}
		}
		else
		{
			stack<int> k;
			for(i=0;i<n;i++)
			{
				cin>>s;
				if(s[0]=='I')
			{
				cin>>m;
				k.push(m);
			}
			else
			{
				if(k.empty())
				cout<<"None";
				else
				{
					cout<<k.top()<<endl;
					k.pop();
				}
			}
			}
		}
	}
	return 0;
}
