#include<iostream>
#include<vector>
using namespace std;
int mian()
{
	vector<int>vec1;
	vector<int>vec2(10);
	int n=100;
	vector<int>vec2_2(n,0);
	vec.size()//获取这个数组大小
	vec2.clear();
	vec2.resize(20,0)//重新分配大小，重新初始化
	 
	return 0;
} 
